# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Cloning Flutter SDK..."
      - run: sudo git clone https://github.com/flutter/flutter.git --depth 1 -b stable ${{ runner.home }}/flutter
      - run: echo "Installing Flutter project dependencies..."
      - run: echo 'PATH="/flutter/bin:$PATH"' >> ~/.bash_profile
      - run: cat ~/.bash_profile
      - run: source ~/.bash_profile
      - run: flutter pub get
      - run: flutter build web
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MINDFUL_PHALANX_473719_J7 }}
          channelId: live
          projectId: mindful-phalanx-473719-j7
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
